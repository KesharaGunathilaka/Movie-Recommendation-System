# Backend dependencies for the Flask recommendation API.
#
# Install from inside this directory:
#     pip install -r requirements.txt
#
# torch installs CPU-only wheels by default. For NVIDIA GPU acceleration
# (roughly 10x faster corpus encoding), install a CUDA build first:
#     pip install torch --index-url https://download.pytorch.org/whl/cu129

flask>=3.1
flask-cors>=6.0
pandas>=2.2
numpy>=2.0
torch>=2.5
sentence-transformers>=5.0
scikit-learn>=1.3
rapidfuzz>=3.12
